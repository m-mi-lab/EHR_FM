# Mortality Prediction Simulation Configuration - ETHOS Methodology
# Matches ETHOS paper: 20 trajectories per patient starting from admission token
data_dir: "/home/sud/temp_/ehr_stuff/EHR_FM/data/tokenized_datasets/mimic_train"
output_dir: "/home/sud/temp_/ehr_stuff/EHR_FM/mortality_results_ethos_methodology"
num_patients: 100  # Evaluate on 100 patients from test set

target_death_patients: 50  # Balanced dataset (50 death, 50 discharge)
num_trajectories_per_patient: 20  # ETHOS uses 20 repetitions

max_tokens: 500  # Safety limit - trajectories stop at death/discharge token
temperature: 1.0
base_seed: 42

models:
  "test_tiny_model":
    path: "/home/sud/temp_/ehr_stuff/EHR_FM/outputs/2025-11-07/07-25-13_model=gpt2_small_monolith,train=tiny,world_size=1/best_model.pt"

